import React from 'react';
import s from './empty-preview.module.scss';

const EmptyPreview = () => (
  <h1 className={s.title}>Start fill your CV</h1>
);

EmptyPreview.propTypes = {};

export default EmptyPreview;
